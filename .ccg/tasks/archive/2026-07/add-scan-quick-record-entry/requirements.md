# 任务

在 Flutter 扫楼页新增一个可见的「记录房源」快速入口，点击进入已有 `QuickRecordPage`，让用户能开始录入月租、拍照和房型。

# 验收

- 扫楼页可见 `记录房源` 入口。
- 点击入口进入 `快速记录` 页面。
- Widget 测试先红后绿。
- flutter analyze/test/build apk 通过。
- 真机 release APK 安装启动后可见入口并可进入快速记录页。
