// 权限服务（占位，尚无实现）。
//
// 职责边界（W1-2+，技术方案 §10.1，UI §8）：基于 permission_handler 封装
// 「用时申请」权限流程——定位（进入扫楼/新建时）、相机（拍照时）、
// 相册（导入时）、麦克风（主动录音时）。拒绝后仍可继续使用降级路径
// （手动地址/文字记录/本地保存）。
//
// 关键约束：绝不在启动时批量申请；拒绝不阻断核心流程，提供「打开设置」入口。
//
// TODO(W1-2): 定义 requestLocation/requestCamera/... 方法与拒绝态处理。
